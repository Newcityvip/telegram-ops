{
  "name": "telegram-ops-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "check": "node --check src/index.js && node --check src/operations.js && node --check src/auth.js && node --check src/users.js && node --check src/responses.js && node --check src/followups.js && node --check src/wallets.js && node --check docs/i18n.js && node --check docs/app.js",
    "hash-password": "node scripts/hash-password.js",
    "test": "node --test",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2"
  }
}
